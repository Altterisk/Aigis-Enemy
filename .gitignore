node_modules/
dist/
test-results/
playwright-report/
.DS_Store
Thumbs.db
*.log

# Served from disk (dev + build both read these straight from public/) but too
# large to push to the public GitHub repo -- stay on disk locally, never
# committed. In production they come from the aigis-assets repo via jsDelivr
# (VITE_ASSETS_CDN in .github/workflows/deploy.yml).
public/sprites/
public/sprite-anim/
public/banners/
public/talk-face/
public/data/units_full.json
