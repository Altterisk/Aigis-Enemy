node_modules/
dist/
.DS_Store
Thumbs.db
*.log

# Served from disk (dev + build both read these straight from public/) but too
# large to push to the public GitHub repo -- stay on disk locally, never
# committed. The unit-page route still exists on GitHub Pages, it just won't
# have images to load.
public/sprites/
public/banners/
public/data/units_full.json
